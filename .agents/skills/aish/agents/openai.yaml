interface:
  display_name: "Aish"
  short_description: "Repo-specific patterns and workflows for aish"
  default_prompt: "Use the aish repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true